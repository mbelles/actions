
name: "Test Deploy"
on: workflow_dispatch

jobs:
  dev-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 100

      - name: Hello World
        uses: ./.github/actions/greet.yml
        with:
          who-to-greet: "Mark"
